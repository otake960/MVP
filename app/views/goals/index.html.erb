<h1>目標一覧</h1>

<%= link_to '新しい目標を追加', new_goal_path, class: "btn btn-success" %>

<% @goals.each do |goal| %>
  <div>
    <% if goal.goal_exercises.any? %>
      <ul>
        <% goal.goal_exercises.each do |ge| %>
          <li>
            種目名: <%= ge.exercise.name %>, 
            目標重量: <%= ge.target_weight %>kg, 
            回数: <%= ge.repetitions %>回
          </li>
        <% end %>
      </ul>
    <% else %>
      <p>この目標には運動が関連付けられていません。</p>
    <% end %>
    <%= link_to '詳細を見る', goal_path(goal), class: "btn btn-primary" %>
    <%= link_to '編集', edit_goal_path(goal), class: "btn btn-secondary" %>
  </div>
<% end %>