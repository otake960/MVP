<h1>New Training Session</h1>

<%= form_with(model: @training_session, local: true) do |form| %>
  <% if @training_session.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@training_session.errors.count, "error") %> prohibited this training session from being saved:</h2>
      <ul>
      <% @training_session.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :date %>
    <%= form.date_field :date, id: :training_session_date %>
  </div>

  <div class="actions">
    <%= form.submit "Save" %>
  </div>
<% end %>

<%= link_to 'Back', training_sessions_path %>
